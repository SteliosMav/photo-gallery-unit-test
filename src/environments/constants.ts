export const FAVORITE_PHOTOS_STORAGE_KEY = 'favoritePhotos'; // Local-storage key for favorite photos array.
export const LOAD_MORE_PIXELS_OFFSET = 200; // The amount of pixels before bottom is reached to load more in infinite-scrolling.
